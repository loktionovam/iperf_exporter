apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: iperf-exporter-remote-node-read
rules:
  - apiGroups:
      - ""
    resources:
      - nodes
    verbs:
      - get
      - list
      - watch
