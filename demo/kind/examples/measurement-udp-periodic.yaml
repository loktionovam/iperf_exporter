apiVersion: netperf.iperfexporter.io/v1
kind: LinkMeasurement
metadata:
  name: udp-periodic-demo
  namespace: iperf-exporter-demo
spec:
  profileRef: udp-quality-periodic
  source:
    cluster: cluster-a
    nodeName: iperf-demo-worker
  destination:
    cluster: cluster-a
    nodeName: iperf-demo-worker2
  directions:
    - sourceToDestination
    - destinationToSource
  networkModes:
    - host
    - pod
    - service
  execution:
    mode: periodicProbe
    every: 5m
    durationSeconds: 180
