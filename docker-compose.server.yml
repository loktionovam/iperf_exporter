services:
  iperf-exporter-server:
    image: ghcr.io/loktionovam/iperf_exporter_server:v3.0.0
    container_name: iperf-exporter-server
    network_mode: host
    environment:
      IPERF_EXPORTER_MODE: server
      IPERF_EXPORTER_LEN: 1200
